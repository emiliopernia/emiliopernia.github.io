import { Box } from '@mui/material'

const Accesories = () => {
  return (
    <Box sx={{height:'600px'}}>
      Accesories (Only available = Home and Products)
      </Box>
  )
}

export default Accesories