import { Box } from '@mui/material'
import React from 'react'

const Accesories = () => {
  return (
    <Box sx={{height:'600px'}}>Accesories (Only available = Home y Products)</Box>
  )
}

export default Accesories